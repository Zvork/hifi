// glsl / C++ compatible source as interface for FadeEffect
#ifdef __cplusplus
#   define J_VEC2 glm::vec2
#else
#   define J_VEC2 vec2
#endif

#define JITTER_SEQUENCE_LENGTH	32

struct Jitter {
	int _currentIndex;
	float _enabled;
	J_VEC2 _scale;
	J_VEC2 _samples[JITTER_SEQUENCE_LENGTH];
};

    // <@if 1@>
    // Trigger Scribe include 
    // <@endif@> <!def that !> 
//

